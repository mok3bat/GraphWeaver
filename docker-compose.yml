version: "3.9"

services:
  graphweaver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graphweaver
    ports:
      - "7860:7860"   # Gradio app
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs